{
  "dependencies": {
    "@radix-ui/react-icons": "^1.3.2"
  },
  "devDependencies": {
    "vite-tsconfig-paths": "^5.1.4"
  },
  "scripts": {
    "postbuild-sign": "codesign --force --deep --sign - ./build/bin/RemTransClient.app",
    "build-and-sign": "wails build && npm run postbuild-sign"
  }
}
